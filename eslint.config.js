// ESLint flat config — 可选安装
// 安装: npm i -D eslint @eslint/js @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint-plugin-react-hooks
// 使用: npx eslint src
//
// 当前项目以 tsc --noEmit (TypeScript strict) 作为主质量门禁，
// ESLint 为可选增强，不在核心依赖中。
export default [];
