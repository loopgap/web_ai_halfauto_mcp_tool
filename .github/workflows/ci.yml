name: CI Pipeline

# ═══════════════════════════════════════════════════════════
# 支持的平台: Windows 10+ (x64) / Ubuntu 22.04+ (x64)
# 不支持: macOS
# ═══════════════════════════════════════════════════════════

on:
  push:
    branches: [main, develop]
    paths:
      - "src/**"
      - "src-tauri/**"
      - "package.json"
      - "scripts/**"
      - "tsconfig*.json"
      - "vite.config.ts"
      - "vitest.config.ts"
      - ".github/workflows/ci.yml"
  pull_request:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUSTFLAGS: "-C debuginfo=0"
  NODE_OPTIONS: "--max-old-space-size=4096"

jobs:
  # ── 安全审计 ──
  security-audit:
    # Ubuntu 22.04 for security audit
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup pnpm
        uses: pnpm/action-setup@v4.3.0
        with:
          version: 10.11.0

      - name: Setup Node.js
        uses: actions/setup-node@v4.3.0
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Security audit
        run: pnpm audit --audit-level=high

      - name: Detailed security report
        run: node scripts/security-audit.mjs

  # ── 前端类型检查 & lint ──
  frontend-check:
    needs: [security-audit]
    # Ubuntu 22.04 for frontend validation
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup pnpm
        uses: pnpm/action-setup@v4.3.0
        with:
          version: 10.11.0

      - name: Setup Node.js
        uses: actions/setup-node@v4.3.0
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: TypeScript check
        run: pnpm exec tsc --noEmit

      - name: MCP Tests
        run: pnpm test -- --run src/__tests__/mcp/
        continue-on-error: true

      - name: Unit tests
        run: pnpm test:ci

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4.4.3
        with:
          name: test-results
          path: |
            test-results.xml
            coverage/
          retention-days: 30

      - name: Vite build (frontend only)
        run: pnpm exec vite build

  # ── 多平台环境检测 (仅 informational，不影响 CI 结果) ──
  # 支持: Ubuntu 22.04, Windows 10/11 (windows-latest)
  platform-doctor:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup pnpm
        uses: pnpm/action-setup@v4.3.0
        with:
          version: 10.11.0

      - name: Setup Node.js
        uses: actions/setup-node@v4.3.0
        with:
          node-version: "24"

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Doctor check (continue on error)
        run: go run task.go doctor || true

  # ── Rust 后端检查 (Linux) ──
  # 注意: os-win 只在 Windows 上完整编译和测试
  backend-check:
    needs: [frontend-check]
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          cache-on-failure: true

      - name: Create dummy dist for Tauri check
        run: mkdir -p dist

      - name: Cargo check
        run: cargo check --jobs 4
        working-directory: src-tauri

      - name: Clippy lint
        run: cargo clippy --jobs 4 -- -D warnings -A dead_code
        working-directory: src-tauri

      - name: Cargo test (portable tests only)
        run: |
          # Run config_tests which are platform-independent
          cargo test --test config_tests --jobs 4
          # Run core crate tests (if any)
          cargo test -p core --jobs 4 || true
          # Run os-linux tests (Linux-specific implementation)
          cargo test -p os-linux --jobs 4
        working-directory: src-tauri

  # ── Rust 后端检查 (Windows) ──
  # Windows 10/11 (x64) - 在 Windows 上运行完整测试，包括 os-win
  backend-check-windows:
    needs: [frontend-check]
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          cache-on-failure: true

      - name: Cargo check
        run: cargo check --jobs 4
        working-directory: src-tauri

      - name: Clippy lint
        run: cargo clippy --jobs 4 -- -D warnings -A dead_code
        working-directory: src-tauri

      - name: Cargo test (full workspace)
        run: cargo test --workspace --jobs 4
        working-directory: src-tauri

      - name: Build MCP standalone server
        run: |
          cargo build --bin mcp_server_main --features tauri --jobs 4
          cargo build --bin mcp_server_main --jobs 4
        working-directory: src-tauri

      - name: Run MCP security tests
        run: cargo test mcp_security --jobs 4
        working-directory: src-tauri

  # ── 治理验证 (独立运行，不依赖其他 job) ──
  governance:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup pnpm
        uses: pnpm/action-setup@v4.3.0
        with:
          version: 10.11.0

      - name: Setup Node.js
        uses: actions/setup-node@v4.3.0
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Governance validate
        run: pnpm governance:validate

      - name: API contract test
        run: pnpm test:governance:api

  # ── Tauri 完整构建 (仅 main 分支，简化依赖) ──
  build-release:
    if: github.ref == 'refs/heads/main'
    needs: [backend-check, backend-check-windows, governance]
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
    runs-on: ${{ matrix.platform }}
    env:
      CARGO_BUILD_JOBS: 4
      NODE_OPTIONS: "--max-old-space-size=4096"
    steps:
      - uses: actions/checkout@v4.2.2

      - name: Setup pnpm
        uses: pnpm/action-setup@v4.3.0
        with:
          version: 10.11.0

      - name: Setup Node.js
        uses: actions/setup-node@v4.3.0
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install system dependencies (Linux)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev patchelf

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
          cache-on-failure: true

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build Tauri app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          projectPath: .

      - name: Upload artifacts
        uses: actions/upload-artifact@v4.4.3
        with:
          name: build-${{ matrix.target }}
          path: |
            src-tauri/target/release/bundle/**/*
          retention-days: 7
